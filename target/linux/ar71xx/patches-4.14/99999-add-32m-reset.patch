Index: linux-4.14.195/drivers/mtd/devices/m25p80.c
===================================================================
--- linux-4.14.195.orig/drivers/mtd/devices/m25p80.c
+++ linux-4.14.195/drivers/mtd/devices/m25p80.c
@@ -27,6 +27,9 @@
 #include <linux/spi/flash.h>
 #include <linux/mtd/spi-nor.h>
 
+#define OPCODE_RSTEN		0x66    /* Enable reset */
+#define OPCODE_RESET		0x99    /* Reset device */
+
 #define	MAX_CMD_SIZE		6
 struct m25p {
 	struct spi_device	*spi;
@@ -313,10 +316,25 @@ static int m25p_remove(struct spi_device
 {
 	struct m25p	*flash = spi_get_drvdata(spi);
 
+	if ((&flash->spi_nor)->addr_width > 3) {
+		printk(KERN_INFO "m25p80: exit 4-byte address mode\n");
+		flash->command[0] = SPINOR_OP_EX4B;
+		spi_write(flash->spi, flash->command, 1);
+		flash->command[0] = OPCODE_RSTEN;
+		spi_write(flash->spi, flash->command, 1);
+		flash->command[0] = OPCODE_RESET;
+		spi_write(flash->spi, flash->command, 1);
+	}
+
 	/* Clean up MTD stuff. */
 	return mtd_device_unregister(&flash->spi_nor.mtd);
 }
 
+void m25p_32m_remove(struct spi_device *spi)
+{
+	m25p_remove(spi);
+}
+
 /*
  * Do NOT add to this array without reading the following:
  *
@@ -391,6 +409,7 @@ static struct spi_driver m25p80_driver =
 	.id_table	= m25p_ids,
 	.probe	= m25p_probe,
 	.remove	= m25p_remove,
+	.shutdown = m25p_32m_remove,
 
 	/* REVISIT: many of these chips have deep power-down modes, which
 	 * should clearly be entered on suspend() to minimize power use.
